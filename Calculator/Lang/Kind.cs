namespace Calculator.Lang {
    public enum Kind {
        Space,
        Number,
        Open,
        Close,
        Plus,
        Minus,
        Mul,
        Div,
        Unknown,
        Eof
    }
}